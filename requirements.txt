# x86 开发机 (RTX 5060 Ti / Blackwell): torch>=2.7 + CUDA 12.6+ 从 pytorch.org。
# Jetson Orin (aarch64): torch/torchvision 必须用 NVIDIA Jetson 专用 wheel
#   (JetPack 6.2 → torch 2.5-2.6 / CUDA 12.6 / py3.10, 需 numpy==1.26.1 + cuSPARSELt),
#   pytorch.org 通用轮子在 Jetson 无 GPU 加速。见 benchmark/deploy_orin_nx_8g.md。
torch>=2.6.0
torchvision>=0.21.0
transformers>=4.44.0
accelerate>=0.34.0
sentencepiece>=0.2.0
pillow>=10.0.0
numpy>=1.24.0
peft>=0.13.0            # stage-2 LoRA (train_sft / evaluate_pope / merge_lora)
pycocoevalcap>=1.2      # captioning 官方指标 (CIDEr/BLEU/METEOR/ROUGE)
gradio>=4.0.0           # app.py demo
pyarrow>=15.0.0         # data/prepare_flickr8k.py 读 parquet
requests>=2.31.0        # data/fetch_coco_images.py 抓 COCO 图
